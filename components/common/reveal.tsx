// Kept as a named alias so existing call sites don't need touching — the
// canonical implementation now lives in the shared motion primitive system.
export { SlideUp as Reveal } from "@/components/motion/slide-up";
